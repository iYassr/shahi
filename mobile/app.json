{
  "expo": {
    "name": "Shahi",
    "slug": "shahi",
    "version": "1.0.0",
    "orientation": "portrait",
    "icon": "./assets/images/icon.png",
    "scheme": "shahi",
    "userInterfaceStyle": "dark",
    "ios": {
      "icon": "./assets/expo.icon",
      "bundleIdentifier": "app.shahi.mobile",
      "infoPlist": {
        "_comment_encryption": "The SSH module bundles OpenSSL and does general-purpose encryption, so this is 'true' — 'false' would be a false declaration. Standard/mass-market crypto (SSH, TLS) qualifies for the export exemption; ANSWER the export-compliance questions in App Store Connect accordingly at submission (or supply the self-classification report). See docs/app-store.md.",
        "ITSAppUsesNonExemptEncryption": true,
        "_comment_ats": "The Direct connection talks plain HTTP to a user-typed server address — a tailnet MagicDNS name (host.tailnet.ts.net) or a raw 100.x tailnet IP, so no fixed domain can be whitelisted. iOS App Transport Security blocks that by default (the error 'requires the use of a secure connection'). Arbitrary loads is allowed because the transport is already encrypted underneath: Tailscale carries it over WireGuard, and the SSH mode tunnels to 127.0.0.1. Justify this in App Store review notes. See docs/app-store.md.",
        "NSAppTransportSecurity": {
          "NSAllowsArbitraryLoads": true
        }
      }
    },
    "android": {
      "adaptiveIcon": {
        "backgroundColor": "#14120F",
        "foregroundImage": "./assets/images/android-icon-foreground.png",
        "backgroundImage": "./assets/images/android-icon-background.png",
        "monochromeImage": "./assets/images/android-icon-monochrome.png"
      },
      "predictiveBackGestureEnabled": false,
      "package": "app.shahi.mobile"
    },
    "web": {
      "output": "static",
      "favicon": "./assets/images/favicon.png"
    },
    "plugins": [
      "expo-router",
      [
        "expo-image-picker",
        {
          "photosPermission": "Shahi attaches a photo from your library to send to the agent you are talking to."
        }
      ],
      [
        "expo-splash-screen",
        {
          "backgroundColor": "#0E0D0B",
          "image": "./assets/images/splash-icon.png",
          "imageWidth": 96
        }
      ],
      "expo-secure-store",
      "expo-notifications",
      [
        "expo-camera",
        {
          "cameraPermission": "Shahi uses the camera to scan the pairing code your server prints.",
          "_comment": "Scanning only: no microphone string and no RECORD_AUDIO, so the app never claims a permission it has no use for.",
          "microphonePermission": false,
          "recordAudioAndroid": false
        }
      ],
      "expo-font",
      "expo-image"
    ],
    "experiments": {
      "typedRoutes": true,
      "reactCompiler": true
    },
    "extra": {
      "eas": {
        "projectId": "fa81100f-5f3c-4c44-898b-4ecd740a84e9"
      },
      "router": {}
    },
    "owner": "yasserd99",
    "runtimeVersion": {
      "policy": "appVersion"
    },
    "updates": {
      "url": "https://u.expo.dev/fa81100f-5f3c-4c44-898b-4ecd740a84e9"
    }
  }
}
