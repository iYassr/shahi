@CLAUDE.md

<!--
  One source of truth, on purpose.

  This file used to be a copy of CLAUDE.md kept for tools that look for
  AGENTS.md. Copies drift: it fell 195 lines behind and started describing a
  project that had moved on, which is worse than not being here at all — an
  agent that reads a stale rule follows a stale rule.

  The line above imports CLAUDE.md, so both names reach the same content and
  there is nothing to keep in step. Edit CLAUDE.md.
-->
